# AGENTS.md

- Run Python with `uv run`, never bare `python`.
- After editing any `.py` file, run `uv run pyright`.
- Write code, comments, and docs in English. All Python must have type annotations.
